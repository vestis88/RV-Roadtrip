# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.tsbuildinfo
*.local
.env
.env.*
!.env.example
# The debug tools' key template — the template only, never the filled-in
# .env.debug.local beside it, which the line above keeps out.
!.env.debug.local.example
.firebase/
test-results/
playwright-report/
blob-report/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scratch git worktrees created for isolated subagent runs. These are whole
# checkouts of this same repository, so committing them would nest the repo
# inside itself; they are created and reclaimed per run and carry nothing
# worth keeping.
.claude/worktrees/
